mod vc_traverser;
mod prf_traverser;

pub use prf_traverser::ProofTraverser;
pub use vc_traverser::VCTraverser;
// pub trait Traverser {
//     fn traverse(&self, ast: ast::Program) -> Vec<dyn Caller>;
// }
